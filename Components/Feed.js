import Posts from './Posts'

// Shows all the data in the feed
const Feed = () => {
    return (
        <main>
            <section>
                <Posts />
            </section>
        </main>
    )
}

export default Feed